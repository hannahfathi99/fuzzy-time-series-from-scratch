# Reproducibility

## Objective
Ensure all experiments can be reproduced exactly using the same pipeline, parameters, and datasets.

---

## Environment Setup

Install dependencies:

```bash
pip install -r requirements.txt
